path "<vpath>/creds/{{identity.entity.aliases.<kerberos>.name}}@<domain>:<role>" {
    capabilities = [ "list", "read" ]
}
path "<vpath>/creds/{{identity.entity.aliases.<kerberos>.name}}:<role>" {
    capabilities = [ "list", "read" ]
}
